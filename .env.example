# Copy to `.env` and fill in values.

# Telegram API (create at https://my.telegram.org/apps)
API_ID=
API_HASH=

# Bot token from @BotFather
BOT_TOKEN=

# Optional legacy session name used when a task has no per-user session.
# Each Telegram user normally gets `rubika_<telegram_user_id>` after `/rubika_connect`.
RUBIKA_SESSION=rubika_session

# Comma-separated Telegram numeric user IDs allowed to use `/admin`
ADMIN_IDS=

# Default split size for large uploads (MB), where applicable
DEFAULT_PART_SIZE_MB=1900

# Reject queued uploads larger than this many MB (Telegram download + zip output).
# Use 0 or leave empty for no limit (helps avoid OOM on small VPS).
MAX_FILE_MB=

# Per-user quotas (SQLite tables user_entitlements + usage_ledger). Set to 1 to disable all limits (only MAX_FILE_MB above may apply).
DISABLE_USAGE_LIMITS=

# Log MD5 checksum per upload attempt (worker logs, adds disk read before Rubika send).
ENABLE_UPLOAD_CHECKSUM=

# Optional HTTP stub payment webhook (see tools/payment_webhook_stub.py).
PAYMENT_WEBHOOK_SECRET=
WEBHOOK_PORT=8787

# Total seconds allowed for one Rubika upload job (all retries). Default 6 hours; raise for multi‑GB ZIPs on slow links.
UPLOAD_TIMEOUT_SECONDS=21600

# Displayed by `/version` and in post-update notifications.
# The installer can bump this on deploy (see APP_BUILD_VERSION in installer.sh).
APP_BUILD_VERSION=dev-local

# If set to 1, true, or yes: skip sending the one-shot “bot updated” message to
# chats listed in `queue/known_chats.json` when the bot starts after a version change.
DISABLE_UPDATE_BROADCAST=

# v2 migration: when 1/true/yes/on/sqlite, wizard/batch state reads SQLite mirror first
# (queue.sqlite3 tables v2_user_state_mirror / v2_batch_session_mirror); JSON is fallback.
# Writes always update both. Default = unset (JSON-first, mirror for recovery only).
V2_EPHEMERAL_READ_PRIMARY_SQLITE=

# Phase 2 billing MVP: /purchase creates a real v2_payments row (StubPaymentGateway) + grant metadata.
BILLING_STUB_CHECKOUT=

# Zarinpal (real PSP). When ZARINPAL_MERCHANT_ID is set, /purchase uses Zarinpal instead of stub.
# Callback must be HTTPS on the Mini App host, e.g. https://your.domain/billing/zarinpal/callback
# Amount is IRR (Rials).
ZARINPAL_MERCHANT_ID=
ZARINPAL_CALLBACK_URL=
ZARINPAL_PLAN_AMOUNT_IRR=500000

# Expire stale pending/initiated payment rows in the background (see v2.billing.reconcile).
BILLING_RECONCILE_ENABLE=
# Seconds between reconcile passes (min 60). Default 600.
BILLING_RECONCILE_INTERVAL_SEC=600
# Age in seconds before pending/initiated rows are marked expired. Default 86400.
BILLING_RECONCILE_PENDING_MAX_AGE_SEC=86400

# Phase 3 transfer: worker (rub.py) pre-validates Rubika session via v2 TransferAdapter before each task.
TRANSFER_V2_VALIDATE=

# Phase 4 toolkit: wave-1 network light: /dns, /myip, /ping (TCP). Default ON when unset in code; installer adds =1 on update.
TOOLKIT_NETWORK_LIGHT=1

# Phase 4 toolkit: hash + Base64: /md5, /sha256, /b64e, /b64d
TOOLKIT_UTILITY_LIGHT=1

# Bale — optional legacy global token (deprecated). Each user should use /bale_connect with their own bot.
BALE_BOT_TOKEN=
BALE_API_BASE=https://tapi.bale.ai
BALE_MAX_FILE_MB=50

# Google Drive — optional legacy global paths (deprecated). Each user: /drive_connect + JSON under secrets/<telegram_id>/.
GOOGLE_DRIVE_SERVICE_ACCOUNT_JSON=
GOOGLE_DRIVE_FOLDER_ID=

# Google Drive OAuth (per-user sign-in). Redirect URI must be MINIAPP_BASE_URL/oauth/google/callback
GOOGLE_DRIVE_OAUTH_CLIENT_ID=
GOOGLE_DRIVE_OAUTH_CLIENT_SECRET=
# Optional override if callback is not under MINIAPP_BASE_URL
GOOGLE_DRIVE_OAUTH_REDIRECT_URI=

# EazyGoogle-style Google Custom Search. Optional; enables /gsearch and /gisearch.
# GOOGLE_CSE_API_KEYS accepts one key or comma-separated keys. GOOGLE_CSE_ID is the Custom Search Engine cx.
GOOGLE_CSE_API_KEYS=
GOOGLE_CSE_ID=

# Cloudflare/EazyFlare uses per-user /cf_connect tokens stored in SQLite.
# No global Cloudflare token is required or recommended.

# Phase 4 toolkit: optional hard ceiling on daily toolkit commands (UTC), all tiers. 0 = use tier only (see user_entitlements.TIER_LIMITS toolkit_daily_cmds).
TOOLKIT_DAILY_LIMIT_PER_USER=0

# Telegram Mini App — browser tools (user's real IP, DNS, latency, utilities).
# MINIAPP_BASE_URL = public HTTPS prefix for the repo `web/` folder (NOT including /miniapp/).
#   Hub URL: {MINIAPP_BASE_URL}/miniapp/index.html
#   Example: https://bot.example.com/tele2rub  → files at https://bot.example.com/tele2rub/miniapp/...
# Production: nginx serves `web/` (MINIAPP_SERVE_LOCAL=0) but proxy `/miniapp/api/` to MINIAPP_PORT for whois/headers.
# Dev/tunnel: MINIAPP_SERVE_LOCAL=1 serves static + API on MINIAPP_PORT.
# HTTP server starts when MINIAPP_BASE_URL or MINIAPP_SERVE_LOCAL is set.
# Bot commands: /myip, /miniapp. See README.md → "Telegram Mini App — setup guide".
MINIAPP_BASE_URL=
MINIAPP_SERVE_LOCAL=
MINIAPP_PORT=8788
# Dev only: allow Mini App API without Telegram initData HMAC (never on public hosts)
MINIAPP_API_OPEN=0
# Optional Fernet/HMAC key for sealing Bale/SSH/CF secrets in SQLite (defaults to BOT_TOKEN)
SECRET_VAULT_KEY=
# Wizard step TTL in seconds (0 = disable expiry). Default 1800.
WIZARD_TTL_SEC=1800

# RSS push notifications (background poll). Default on when unset in code.
RSS_POLL_ENABLE=1
RSS_POLL_INTERVAL_SEC=900

# Optional RSSHub base for X/Twitter → RSS (default https://rsshub.app)
RSSHUB_BASE_URL=https://rsshub.app

# World daily digest (Tehran local hour) for digest/push feeds
WORLD_DIGEST_ENABLE=1
WORLD_DIGEST_HOUR_TEHRAN=8
# Optional overrides for per-tier feed caps (defaults come from user_entitlements.TIER_LIMITS)
WORLD_FEED_LIMIT_GUEST=5
WORLD_FEED_LIMIT_FREE=20
WORLD_FEED_LIMIT_PRO=50
WORLD_FEED_LIMIT_STAR=100
# Optional rates JSON override (rates.IRR for USD base). Primary source is TGJU free market.
FX_IRR_PROVIDER_URL=

# Netscape cookies.txt for yt-dlp (YouTube bot-check bypass)
# Export from a logged-in browser: https://github.com/yt-dlp/yt-dlp/wiki/Extractors#exporting-youtube-cookies
# Example: YTDLP_COOKIES=/opt/tele2rub/secrets/youtube.cookies.txt
YTDLP_COOKIES=

# Free daily market digest (gold/USD brief). Advanced alerts remain Pro/Star-only.
MARKET_DIGEST_ENABLE=1
MARKET_DIGEST_HOUR_TEHRAN=9

# Pro alerts: default daily/weekly send hour (Tehran) + quiet window (scheduled only; spikes still fire).
ALERT_DEFAULT_HOUR_TEHRAN=9
ALERT_QUIET_START_TEHRAN=23
ALERT_QUIET_END_TEHRAN=7
